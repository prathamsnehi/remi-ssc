What I have done so far in the face recognition pipeline:

- set up the skeleton logic to detect faces and generate embeddings
- now all that I need to do is to compare among all of the person embeddings stored
- - find a match: update the detector with allat information -> and perhaps don't do any more face recognition logic as long as that face is in frame? =? not implementing the face recognition pipeline block stuff just yet, that's like a later optimization thing
  - don't find a match: keep the 0.5s detection intact (no blocks here), and update the detector such that it shows that "yes I see a person, and there is no identified person, that means the person has to be a non-match new person in the camera type beat"
